# Biome summary (repo-wide)

- Tabs, line width 100, LF, double quotes, semicolons, trailing commas all.
- `organizeImports` enabled.
- Nest folder: `unsafeParameterDecoratorsEnabled: true`.
- Ignore `**/generated/**`, build output, `node_modules`.
  (Adjust rule toggles per team; keep JSON lint off if noisy.)
